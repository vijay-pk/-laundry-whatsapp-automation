# Dependencies
node_modules/

# Environment secrets — never commit
.env
.env.*
!.env.example

# Local editor / agent tooling state
.claude/settings.local.json
.kilo/

# Logs & OS files
logs/
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Local development database (npm run db:local)
.localdb/

# Built public website (npm run site:build)
site/dist/

# Database CA certificates downloaded for local setup (belong in hosting env vars)
*.crt
*.pem
